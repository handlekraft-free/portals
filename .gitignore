node_modules
dist
.DS_Store
server/public
vite.config.ts.*
*.tar.gz

# Internal / org-specific files (never commit to OSS repo)
attached_assets/
server/ai-briefing.local.md

# Local env / secrets — never commit
.env
.env.*
!.env.example

# Local upload directory (dev only)
data/uploads/

# Editor / OS
.vscode/
.idea/
*.swp

# Replit-local / agent scratch — should not be on GitHub
.cache/
.upm/
.config/
.local/
.agents/
attached_assets/
